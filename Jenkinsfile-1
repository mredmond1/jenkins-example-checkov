pipeline {
  agent {
    docker { image: bridgecrew/checkov }
    }
  }
  stages {
    stage('verify version') {
      steps {
        container('checkov') {
          sh 'checkov --version'
        }
      }
    }
  }
  
