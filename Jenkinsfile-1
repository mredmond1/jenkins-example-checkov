pipeline {
  agent {
    docker { image 'bridgecrew/checkov:latest' }
  }
  stages {
    stage('verify version') {
      steps {
          sh 'checkov -d https://github.com/mredmond1/jenkins-example-checkov/compliant --quiet'
         }
       }
     }
   }
 
  
